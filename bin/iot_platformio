#!/usr/bin/env bash

[ "$IOTEMPOWER_ACTIVE" = "yes" ] || { echo "IoTempower not active, aborting." 1>&2;exit 1; }

# Running in Termux check
GRUN=""
if [[ -n "$PREFIX" ]] && command -v apt-get &> /dev/null; then
  GRUN='LD_PRELOAD=""'
fi

exec $GRUN "$IOTEMPOWER_VPYTHON/bin/pio" "$@"
