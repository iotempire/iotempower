#!/bin/bash
# esp port prober
# author: ulno creation date
#
# tries to auto probe the port where esp might be connected, using esptool
# prints out the first port which might have an esp installed

# find directory
dir=$(cd -P -- "$(dirname -- "$0")" && pwd -P)

dir/check_dependencies && exit 1

