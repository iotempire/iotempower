!!help("help")

File manipulation commands (imported from upysh):
pwd, cd("new_dir"), ls, ls(...), head(...), cat(...)
newfile(...), mv("old", "new"), rm(...), mkdir(...), rmdir(...),
clear
To see this help text again, type "man".
